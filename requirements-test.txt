# Backend unit-test dependencies. The tests import the real Home Assistant
# package (the integration modules import `homeassistant.*` at load time).
#
# Pinned to the newest released HA; the dev container tracks HA `dev`, which is
# not published to PyPI. HA 2026.7.x requires Python >=3.14.2.
homeassistant==2026.7.2
pytest
